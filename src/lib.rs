// Declare the wallet and blockchain modules to be accessible from the crate root
// Expose the wallet, blockchain, and token modules
pub mod wallet;
pub mod blockchain;
pub mod token;  // If you have a separate file for Token, add it here
